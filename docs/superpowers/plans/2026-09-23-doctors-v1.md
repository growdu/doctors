# 陪诊师平台 v1.0 MVP 实施计划（阶段 0~4）

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:test-driven-development / superpowers:verification-before-completion
>
> **Goal:** 在 monorepo 内交付"骨架 + 共享库 + auth-service + order-service + match-service"，并实现端到端可跑的"患者登录 → 下单 → 抢单"链路。
>
> **Architecture:** Go 1.22 单 module + PostgreSQL 16 + Redis 7 + Kafka；服务间通过 Kafka 解耦，同步通过 gRPC/REST；共享 `shared/` 包。
>
> **Tech Stack:** Go · pgx + sqlc · go-redis · kafka-go · testify · dockertest

## Global Constraints

- **语言版本**：Go 1.22+
- **Lint**：golangci-lint v1.59+
- **测试覆盖率**：业务包 ≥ 80%
- **DB**：PostgreSQL 16（用 `FOR UPDATE SKIP LOCKED` 做抢单）
- **事务**：任何写操作必须显式事务（`shared/db.WithTx`）
- **响应格式**：`{"code":0,"message":"ok","data":{},"trace_id":"..."}`（`shared/httpx`）
- **错误码**：业务码 5 位，系统码 6 位
- **Commit**：每个测试 + 实现通过立即 commit；前缀 feat/test/fix/chore/docs
- **Branch**：直接在 `main` 推进（小项目，main 即开发线）

---

## 阶段 0 · 项目骨架（`feat: skeleton`）

### Task 0.1: go.mod + 顶层目录

**Files:**
- Create: `go.mod`
- Create: `.gitignore`
- Create: `.golangci.yml`
- Create: `Makefile`

**Step 1**: 写 `go.mod`（单 module `github.com/growdu/doctors`）
**Step 2**: 写 `.gitignore`（覆盖 `/bin/`、`.idea/`、`.env`、覆盖 `docs/diagrams/.svg-cache` 等）
**Step 3**: 写 `.golangci.yml`（启用 `govet`/`errcheck`/`gosimple`/`staticcheck`/`ineffassign`/`unused`）
**Step 4**: 写 `Makefile` 含 target：`help / test / test-integration / lint / docker-up / docker-down / run-auth / run-order / run-match / migrate`
**Step 5**: 跑 `go mod tidy` 确认无 error
**Step 6**: Commit `chore: skeleton`

### Task 0.2: docker-compose

**Files:**
- Create: `docker-compose.yml`

**服务**：postgres:16-alpine、redis:7-alpine、bitnami/kafka:3.7、bitnami/zookeeper
- 健康检查 + `depends_on: condition: service_healthy`
- 端口暴露：5432 / 6379 / 9092
- 卷：`./.data/postgres`、`./.data/kafka`
**Commit**: `chore: docker-compose for local dev`

### Task 0.3: shared/httpx

**Files:**
- Create: `shared/httpx/response.go`
- Test: `shared/httpx/response_test.go`

**接口**：
```go
type Resp[T any] struct { Code int `json:"code"`; Message string `json:"message"`; Data T `json:"data,omitempty"`; TraceID string `json:"trace_id"` }
func OK[T any](c *gin.Context, data T)
func Fail(c *gin.Context, code int, msg string)
```

**TDD**：
- RED：`TestOK_ReturnsJSONWithCode0` — 断言响应 code=0 / message="ok" / data=传入值 / trace_id 不为空
- GREEN：实现 `OK` 与 `Fail`
- 跑：`go test ./shared/httpx/...` PASS
- **Commit**：`feat: shared/httpx 统一响应格式 + 单测`

### Task 0.4: shared/errs

**Files:**
- Create: `shared/errs/code.go`
- Test: `shared/errs/code_test.go`

**接口**：
```go
type Code int
const (
    CodeOK Code = 0
    CodeParamInvalid Code = 10001
    CodeUnauthorized Code = 11001
    // 业务码 5 位、系统码 6 位
)
func (c Code) String() string
func (c Code) HTTPStatus() int
```

**TDD**：
- RED：断言 `CodeParamInvalid.HTTPStatus() == 400`
- GREEN：实现
- **Commit**：`feat: shared/errs 错误码 + 单测`

### Task 0.5: 顶层 smoke

- `make help` 输出所有 target
- `go test ./...` PASS
- `make docker-up` 启动 PG/Redis/Kafka
- `make docker-down` 停止
- **Commit**：`chore: 验证阶段 0`

---

## 阶段 1 · shared 基础设施（`feat: shared`）

### Task 1.1: shared/config

**Files:**
- Create: `shared/config/loader.go`
- Test: `shared/config/loader_test.go`

**接口**：
```go
type Config struct { Service string; HTTP HTTP; DB DB; Redis Redis; Kafka Kafka; Auth Auth }
func Load(service string) (*Config, error) // 从 ./config/<service>.yaml + 环境变量
```

**TDD**：mock yaml 文件 + env override；断言字段正确解析。
**Commit**: `feat: shared/config 配置加载 + 单测`

### Task 1.2: shared/logger

**Files:**
- Create: `shared/logger/logger.go`
- Test: `shared/logger/logger_test.go`

**接口**：`func L() *zap.Logger`（全局 JSON 输出 + trace_id 注入）
**TDD**：断言日志输出含 trace_id 字段
**Commit**: `feat: shared/logger + 单测`

### Task 1.3: shared/db

**Files:**
- Create: `shared/db/pool.go`
- Test: `shared/db/pool_integration_test.go`（用 dockertest 起 PG）

**接口**：
```go
func NewPool(ctx context.Context, cfg DB) (*pgxpool.Pool, error)
func WithTx(ctx context.Context, pool *pgxpool.Pool, fn func(pgx.Tx) error) error
func HealthCheck(ctx context.Context, pool *pgxpool.Pool) error
```

**TDD**：集成测试用 docker-compose 起的 PG；断言 ping OK + WithTx commit/rollback
**Commit**: `feat: shared/db pgxpool + 事务辅助 + 集成测试`

### Task 1.4: shared/redis

**Files:**
- Create: `shared/redis/client.go`
- Test: `shared/redis/client_integration_test.go`

**接口**：`func NewClient(cfg Redis) *redis.Client`；`HealthCheck`；`Close`
**TDD**：集成测试 ping + set/get
**Commit**: `feat: shared/redis + 集成测试`

### Task 1.5: shared/kafka

**Files:**
- Create: `shared/kafka/producer.go`、`consumer.go`
- Test: `shared/kafka/*_integration_test.go`

**接口**：
```go
type Producer interface { Send(ctx, topic string, key, value []byte) error; Close() error }
type Consumer interface { Subscribe(ctx, topics []string, handler func(msg Message) error) error; Close() error }
```

**TDD**：集成测试 produce + consume 闭环
**Commit**: `feat: shared/kafka + 集成测试`

### Task 1.6: shared/auth (JWT)

**Files:**
- Create: `shared/auth/jwt.go`
- Test: `shared/auth/jwt_test.go`

**接口**：
```go
type Claims struct { UserID int64; Role string; UnionID string; jwt.RegisteredClaims }
func Sign(secret string, claims Claims) (string, error)
func Parse(secret, token string) (*Claims, error)
```

**TDD**：单元测试 sign+parse round trip + 过期 + 错误签名
**Commit**: `feat: shared/auth JWT + 单测`

### Task 1.7: shared/idempotency

**Files:**
- Create: `shared/idempotency/idem.go`
- Test: `shared/idempotency/idem_test.go`

**接口**：
```go
func Key(header string) string // Idempotency-Key → 规范化
type Store interface { Reserve(ctx, key string, ttl time.Duration) (bool, error); Done(ctx, key string) error }
```

**TDD**：用内存 store；Reserve 第二次返回 false；Done 后可再 Reserve
**Commit**: `feat: shared/idempotency + 单测`

---

## 阶段 2 · auth-service（`feat: auth-service`）

### Task 2.1: 服务骨架

**Files:**
- Create: `services/auth/cmd/main.go`
- Create: `services/auth/internal/server/server.go`
- Create: `services/auth/internal/router/router.go`
- Test: `services/auth/internal/router/router_test.go`

**路由**：
- `POST /api/v1/auth/sms/send`
- `POST /api/v1/auth/login`
- `POST /api/v1/auth/refresh`
- `POST /api/v1/users/real-name/auth`
- `GET /api/v1/users/me`
- `GET /healthz`

**TDD**：
- RED：`TestHealthz_ReturnsOK`
- GREEN：实现 healthz
- RED：`TestRouter_RegistersAuthRoutes`（断言路由表）
- GREEN：实现 router
- **Commit**：`feat: auth-service 骨架 + 路由 + healthz`

### Task 2.2: 用户表迁移

**Files:**
- Create: `migrations/0001_users.up.sql`、`0001_users.down.sql`
- Test: `migrations/0001_users_test.go`（integration）

**SQL**（PG 16）：
```sql
CREATE TABLE users (
  id BIGSERIAL PRIMARY KEY,
  phone VARCHAR(20) UNIQUE NOT NULL,
  role VARCHAR(16) NOT NULL CHECK (role IN ('patient','escort','admin')),
  nickname VARCHAR(64),
  avatar_url VARCHAR(255),
  real_name_verified BOOLEAN NOT NULL DEFAULT FALSE,
  id_card_hash VARCHAR(64),
  id_card_tail VARCHAR(8),
  wx_unionid VARCHAR(64) UNIQUE,
  wx_openid_mini VARCHAR(64),
  wx_openid_app VARCHAR(64),
  status SMALLINT NOT NULL DEFAULT 1,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  deleted_at TIMESTAMPTZ
);
CREATE INDEX idx_users_phone ON users(phone);
CREATE INDEX idx_users_wx_unionid ON users(wx_unionid);
CREATE INDEX idx_users_role_status ON users(role, status) WHERE deleted_at IS NULL;
```

**Commit**: `feat: migrations 0001 users`

### Task 2.3: 用户仓储 (sqlc)

**Files:**
- Create: `services/auth/internal/repo/user_repo.go`
- Create: `services/auth/internal/repo/user.sql` (sqlc query)
- Create: `services/auth/sqlc.yaml`
- Test: `services/auth/internal/repo/user_repo_integration_test.go`

**TDD**：集成测试 create + find by phone / unionid
**Commit**: `feat: auth-service user repo + 集成测试`

### Task 2.4: 短信发送（mock）

**Files:**
- Create: `services/auth/internal/sms/sender.go`
- Test: `services/auth/internal/sms/sender_test.go`

**接口**：`type Sender interface { Send(ctx, phone, code string) error }`
- 默认实现 `LogSender`（写日志 + 内存 map，开发可查）
**Commit**: `feat: auth-service 短信接口（mock 默认实现）+ 单测`

### Task 2.5: 微信登录（mock）

**Files:**
- Create: `services/auth/internal/wxlogin/client.go`
- Test: `services/auth/internal/wxlogin/client_test.go`

**接口**：`type Client interface { Code2Session(ctx, code string) (unionid, openid, err) }`
- 默认实现 `MockClient`：根据 code 前缀返回固定 unionid
**Commit**: `feat: auth-service 微信登录接口（mock）+ 单测`

### Task 2.6: 实名（mock）

**Files:**
- Create: `services/auth/internal/realname/verifier.go`
- Test: `services/auth/internal/realname/verifier_test.go`

**接口**：
```go
type Verifier interface {
  Verify(ctx, name, idCard string) (verified bool, hash string, tail string, err error)
}
```
- 默认 mock：任何 name+idCard 非空即通过，hash = sha256(idCard+salt)，tail = 后四位
**Commit**: `feat: auth-service 实名接口（mock）+ 单测`

### Task 2.7: Auth Service 业务层

**Files:**
- Create: `services/auth/internal/service/auth_service.go`
- Test: `services/auth/internal/service/auth_service_test.go` + `auth_service_integration_test.go`

**接口**：
```go
type AuthService interface {
  SendSMS(ctx, phone string) error
  LoginBySMS(ctx, phone, code string) (token string, user *User, err error)
  LoginByWX(ctx, code string) (token string, user *User, err error)
  Refresh(ctx, refreshToken string) (token string, err error)
  RealNameAuth(ctx, userID int64, name, idCard string) error
  Me(ctx, userID int64) (*User, error)
}
```

**TDD**：
- RED `TestLoginBySMS_CreatesUserIfNotExist`：首次登录创建用户；再次登录命中
- GREEN：实现 + 校验手机号格式
- RED `TestLoginByWX_BindsToExistingUserByUnionid`
- GREEN：实现 unionid 绑定
- RED `TestRealNameAuth_StoresHashAndTail`（断言 hash 一致、tail 末四位、不存明文）
- GREEN：实现
- **Commit**：`feat: auth-service 业务层 + 单元 + 集成测试`

### Task 2.8: HTTP handlers + main

**Files:**
- Modify: `services/auth/internal/server/server.go`
- Create: `services/auth/internal/handler/*.go`
- Test: `services/auth/internal/handler/*_test.go`

**TDD**：每个 handler 一个表驱动测试
**Commit**：`feat: auth-service HTTP handlers + handler 测试`

### Task 2.9: 启动 + smoke

- `make docker-up`
- `make run-auth`
- curl `POST /api/v1/auth/sms/send`、`POST /api/v1/auth/login`、`GET /api/v1/users/me`
- **Commit**：`test: auth-service 端到端 smoke`

---

## 阶段 3 · order-service（`feat: order-service`）

### Task 3.1: 服务骨架 + 路由

**Files:**
- Create: `services/order/cmd/main.go`
- Create: `services/order/internal/server/server.go`
- Create: `services/order/internal/router/router.go`
- Test: `services/order/internal/router/router_test.go`

**路由**：
- `POST /api/v1/orders`
- `GET /api/v1/orders`
- `GET /api/v1/orders/{id}`
- `POST /api/v1/orders/{id}/accept`
- `POST /api/v1/orders/{id}/cancel`
- `POST /api/v1/orders/{id}/finish`
- `GET /healthz`

**Commit**: `feat: order-service 骨架 + 路由`

### Task 3.2: order 表迁移

**Files:**
- Create: `migrations/0002_orders.up.sql`、`0002_orders.down.sql`

```sql
CREATE TABLE orders (
  id BIGSERIAL PRIMARY KEY,
  order_no VARCHAR(32) UNIQUE NOT NULL,
  patient_id BIGINT NOT NULL REFERENCES users(id),
  hospital_id BIGINT NOT NULL,
  package_id BIGINT NOT NULL,
  service_start_at TIMESTAMPTZ NOT NULL,
  amount NUMERIC(10,2) NOT NULL,
  final_amount NUMERIC(10,2) NOT NULL,
  status VARCHAR(16) NOT NULL CHECK (status IN (
    'created','paid','matching','accepted','in_service','completed',
    'reviewed','refunding','refunded','closed'
  )),
  version INT NOT NULL DEFAULT 0,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  deleted_at TIMESTAMPTZ
);
CREATE TABLE order_events (
  id BIGSERIAL PRIMARY KEY,
  order_id BIGINT NOT NULL REFERENCES orders(id),
  from_status VARCHAR(16),
  to_status VARCHAR(16) NOT NULL,
  actor_id BIGINT,
  payload JSONB,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
CREATE INDEX idx_orders_patient_created ON orders(patient_id, created_at DESC);
CREATE INDEX idx_orders_status_start ON orders(status, service_start_at)
  WHERE status IN ('paid','matching','accepted','in_service');
```

**Commit**: `feat: migrations 0002 orders + order_events`

### Task 3.3: 状态机（纯函数）

**Files:**
- Create: `services/order/internal/state/machine.go`
- Test: `services/order/internal/state/machine_test.go`

**接口**：
```go
func CanTransition(from, to Status) bool
var transitions = map[Status][]Status{...}
```

**TDD**：表驱动测试所有合法 / 非法转换
**Commit**: `feat: order-service 状态机 + 单测`

### Task 3.4: order repo + sqlc

**Files:**
- Create: `services/order/internal/repo/order_repo.go`
- Create: `services/order/internal/repo/order.sql`
- Test: `services/order/internal/repo/order_repo_integration_test.go`

**TDD**：create / find / update-status（乐观锁）/ insert-event
**Commit**: `feat: order-service repo + 集成测试`

### Task 3.5: 创建订单业务逻辑

**Files:**
- Create: `services/order/internal/service/order_service.go`
- Test: `services/order/internal/service/order_service_test.go`

**接口**：`func (s *Service) Create(ctx, req CreateReq) (*Order, error)`
- 校验实名 + 时段 + 金额
- 写 orders（status=created）+ order_events
**TDD**：表驱动测试正常 / 缺实名 / 时间冲突 / 金额负数
**Commit**: `feat: order-service 创建订单业务 + 单测`

### Task 3.6: 抢单并发（核心）

**Files:**
- Create: `services/order/internal/service/accept.go`
- Test: `services/order/internal/service/accept_integration_test.go`

**核心 SQL**：
```go
tx, _ := pool.Begin(ctx)
defer tx.Rollback(ctx)

var status string; var version int
err := tx.QueryRow(ctx, `
  SELECT status, version FROM orders
   WHERE id = $1
   FOR UPDATE SKIP LOCKED
`, orderID).Scan(&status, &version)
if status != "paid" && status != "matching" { return ErrInvalidState }

_, err = tx.Exec(ctx, `
  UPDATE orders SET status='accepted', escort_id=$1, version=version+1, updated_at=NOW()
   WHERE id=$2 AND version=$3
`, escortID, orderID, version)
tx.Commit(ctx)
```

**TDD**：
- 集成测试 1：单 escort 抢单成功
- 集成测试 2：100 个并发抢单，恰好 1 个成功
- 集成测试 3：抢单 5 分钟内未签到，订单回到 matching
**Commit**: `feat: order-service 抢单并发 + 集成测试`

### Task 3.7: Kafka 发布 + 监听

**Files:**
- Create: `services/order/internal/events/publisher.go`
- Test: `services/order/internal/events/publisher_integration_test.go`

**接口**：
```go
type Publisher interface {
  PublishOrderCreated(ctx, order *Order) error
  PublishOrderAccepted(ctx, order *Order) error
}
```

**Commit**: `feat: order-service 事件发布 + 集成测试`

### Task 3.8: HTTP handlers

**Files:**
- Create: `services/order/internal/handler/*.go`
- Test: `services/order/internal/handler/*_test.go`

**Commit**: `feat: order-service HTTP handlers`

### Task 3.9: smoke

- 起 PG → 跑 migrate
- `make run-order`
- curl：`POST /orders`（未实名拦截）→ 写实名 → 重试成功 → `POST /orders/{id}/accept`
- **Commit**: `test: order-service 端到端 smoke`

---

## 阶段 4 · match-service（`feat: match-service`）

### Task 4.1: 服务骨架 + 路由

**Files:**
- Create: `services/match/cmd/main.go`、`server.go`、`router.go`
- Test: `router_test.go`

**路由**：
- `GET /api/v1/match/feed`
- `POST /api/v1/match/candidates`
- `POST /internal/match/dispatch`（订单回调）
- `GET /healthz`

**Commit**: `feat: match-service 骨架`

### Task 4.2: 候选计算

**Files:**
- Create: `services/match/internal/scorer/scorer.go`
- Test: `scorer_test.go`

**接口**：
```go
type Candidate struct { EscortID int64; Score float64 }
func Compute(ctx, order *Order, escorts []Escort) []Candidate
```

**TDD**：城市 / 时段 / 评分 / 距离权重；排序后返回
**Commit**: `feat: match-service 候选计算 + 单测`

### Task 4.3: Redis 抢单池

**Files:**
- Create: `services/match/internal/pool/pool.go`
- Test: `pool_integration_test.go`

**接口**：
```go
type Pool interface {
  Push(ctx, orderID, escortsJSON string, expire time.Duration) error
  Pop(ctx, orderID, escortID int64) (bool, error) // 用 Lua 原子
  Peek(ctx, orderID, topN int) ([]int64, error)
}
```

**TDD**：集成测试 push/peek/pop 原子性
**Commit**: `feat: match-service 抢单池 + 集成测试`

### Task 4.4: Kafka 消费

**Files:**
- Create: `services/match/internal/consumer/order_consumer.go`
- Test: `consumer_integration_test.go`

**接口**：订阅 `order.created` → 查候选 → 写 Redis 池 → 推 mock sender
**Commit**: `feat: match-service Kafka 消费 + 集成测试`

### Task 4.5: HTTP handlers

**Files:**
- Create: `services/match/internal/handler/*.go`
- Test: handler 测试

**Commit**: `feat: match-service HTTP handlers`

### Task 4.6: 端到端 smoke

- 起 PG/Redis/Kafka
- 起 auth + order + match
- curl：登录 → 下单（match 消费 order.created 写池）→ 陪诊师 GET /feed → POST /orders/{id}/accept
- 验证 status 流转：created → matching → accepted
- **Commit**: `test: 端到端 smoke`

---

## 验证清单（阶段 4 收尾时必须全部通过）

- [ ] `make lint` 通过
- [ ] `make test` 单元测试全部通过
- [ ] `make test-integration` 集成测试全部通过
- [ ] `make docker-up && make run-auth & make run-order & make run-match` 三个服务同时跑起来
- [ ] 端到端 smoke 脚本 `scripts/smoke.sh` 跑通
- [ ] dev.md 更新到当前状态

---

## 推送策略

- 每个 Task 完成后 `git push origin main`
- 推送前 `git status` 干净
- 失败时立刻停下来诊断，不掩盖