# lib/features/

**领域页面 + 领域 provider 容器**（v1 骨架占位）。

按 spec §2 设计，后续 Task 4+ 在此处落地：

- `lib/pages/splash/`、`lib/pages/auth/`、`lib/pages/onboarding/`、`lib/pages/audit/`
- `lib/pages/home/`（`invitations_page.dart` / `availability_page.dart` / `orders_page.dart` / `wallet_page.dart` / `profile_page.dart`）
- `lib/pages/order/`（详情 / 签到 / 打卡）
- `lib/pages/wallet/`（提现 / 交易记录）
- `lib/pages/sos/`（SOS 触发）
- `lib/pages/message/`（站内信 P2）
- `lib/pages/profile/`（资料编辑 / 评价 P1）

> Task 1~3 不实现业务页面，仅保留目录结构。